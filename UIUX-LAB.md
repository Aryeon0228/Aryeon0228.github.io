# UI/UX Lab

시지각 원리와 사용성 법칙을 직접 조작하고 비교하는 실험실입니다. 기존 Studio Penumbra GitHub Pages 사이트에 들어가는 정적 페이지이며, 빌드 과정이나 서버 데이터베이스가 필요하지 않습니다.

## 실험 구성

| 분류 | 실험 | 조작·관찰 |
| --- | --- | --- |
| 01 · 시지각의 원리 | 근접성·유사성·폐쇄성·연속성·공동운명 | 간격, 색·형태, 윤곽의 끊김, 교차각, 움직임 관계 |
| 01 · 시지각의 원리 | 시각적 위계 | 같은 예약 화면의 크기·대비·여백·정렬 |
| 02 · 사용성의 법칙 | 힉 | 선택지 수에 따른 정답 선택 완료 시간과 오답 |
| 02 · 사용성의 법칙 | 피츠 | 목표 너비 또는 거리에 따른 이동 시간과 빗나감 |
| 02 · 사용성의 법칙 | 제이콥 | 연습한 메뉴 배치와 순서를 바꾼 배치 |
| 02 · 사용성의 법칙 | 밀러와 청킹 | 같은 노출 시간에서 숫자의 시각적 구분과 회상 |
| 03 · 화면 진단 | 닐슨 10원칙 | 장비 예약 서비스의 10가지 A/B 설계 사례 |

각 실험에 관찰 질문, **서비스 UI / 게임 UI** 적용 예시와 원문 참고 링크가 있습니다. 적용 예시 전환은 같은 원리를 두 사용 맥락에서 비교하는 기능입니다. 측정치는 조건에 따른 차이를 관찰하기 위한 것으로, 통제된 연구 결과나 자동 사용성 평가 점수가 아닙니다.

## 실행과 사용

이 폴더에서 다음 명령을 실행한 뒤 `http://127.0.0.1:8767/uiux-lab.html`을 엽니다.

```sh
python3 -m http.server 8767 --bind 127.0.0.1
```

실험 목록이나 이전·다음 링크로 11개 실험을 이동합니다. 좁은 화면에서는 실험 선택 메뉴를 사용합니다. 현재 실험이 목록의 보이는 영역 밖에 있으면 목록이 자동으로 스크롤됩니다. **실험 초기화**는 현재 실험의 조절값과 측정치를 초기 상태로 되돌립니다.

**실험 링크 복사**는 현재 원리로 바로 연결되는 주소를 복사합니다. 닐슨은 선택한 원칙 번호도 링크에 포함됩니다. 링크는 실험의 조절값이나 측정 기록을 저장하지 않습니다.

관찰 메모는 **기록 저장**을 눌러 현재 브라우저의 localStorage에 보관합니다. 다른 기기와 동기화되지 않습니다. **전체 기록 내려받기**는 저장 여부와 관계없이 현재까지 작성한 메모를 Markdown으로 저장합니다. 반응 시간·회상 점수 등 실험 측정치는 해당 실험을 열어둔 동안만 유지되므로, 남기고 싶은 결과는 관찰 메모에 적습니다.

## 파일 구성

- `uiux-lab.html / .css / .mjs`: 공통 화면, 실험 이동, 적용 예시 전환, 메모
- `uiux-perception.mjs / .css`: 시지각 6개 실험
- `uiux-behavior.mjs / .css`: 사용성 법칙 4개 실험
- `uiux-review.mjs / .css`: 닐슨 10원칙 사례
- `penumbra-ui.css / .js`, `lab-navigation.css`: 기존 사이트의 공통 파일 사용

홈페이지 Work 카드와 기존 5개 Lab 메뉴에서 새 페이지로 연결됩니다.

## 디자인

기존 Labs와 같은 검정 바탕, 회색 패널, 흰색 제목을 사용합니다. 제목 앞 장식과 주차 표기를 없애고 실험을 세 분류로 나눴습니다. 별도의 수업 모드 없이 동일한 화면에서 실험, 관찰 질문, 서비스·게임 적용 예시를 살펴봅니다. 실험 안의 색·형태·크기·거리 등 비교 조건은 각 원리를 보여주는 데 사용합니다.

## 확인 절차

다음 항목은 변경 후 브라우저에서 확인할 기능과 화면입니다.

- 데스크톱과 모바일에서 실험 선택, 이전·다음 이동, 현재 실험의 목록 자동 스크롤
- 11개 실험의 초기화와 서비스 UI / 게임 UI 적용 예시 전환
- 공동운명 재생·정지, 피츠 클릭 기록, 밀러 가리기·회상 채점
- 닐슨의 A/B 사례, 상태 피드백, 오류 예방, 복구 안내와 키보드 포커스 연결
- 실험 링크 복사와 닐슨 원칙 번호가 포함된 링크로 직접 접근
- 메모 저장·새로고침 후 복원 및 Markdown 다운로드
- 모바일 가로 넘침, 내용 잘림, 조절 요소 접근성

정적 검사:

```sh
node --check uiux-lab.mjs
node --check uiux-perception.mjs
node --check uiux-behavior.mjs
node --check uiux-review.mjs
git diff --check
```
