# UI/UX Lab

강의계획서의 2주차·4주차 핵심 이론을 조작하고 비교하는 첫 버전입니다. 기존 Studio Penumbra GitHub Pages 사이트에 들어가는 정적 페이지이며, 빌드 과정이나 서버 데이터베이스가 필요하지 않습니다.

## 수업 범위

| 주차 | 실험 | 조작·관찰 |
| --- | --- | --- |
| 2 | 근접성·유사성·폐쇄성·연속성·공동운명 | 간격, 색·형태, 윤곽의 끊김, 교차각, 움직임 관계 |
| 2 | 시각적 위계 | 같은 예약 화면의 크기·대비·여백·정렬 |
| 4 | 힉 | 선택지 수에 따른 정답 선택 완료 시간과 오답 |
| 4 | 피츠 | 목표 너비 또는 거리에 따른 이동 시간과 빗나감 |
| 4 | 제이콥 | 연습한 메뉴 배치와 순서를 바꾼 배치 |
| 4 | 밀러와 청킹 | 같은 노출 시간에서 숫자의 시각적 구분과 회상 |
| 4 | 닐슨 10원칙 | 장비 예약 서비스의 10가지 A/B 설계 사례 |

각 실험에 관찰 질문, 서비스·게임 적용 예시와 원문 참고 링크가 있습니다. 기록은 개인의 수업 관찰을 돕기 위한 것으로, 통제된 연구 결과나 자동 사용성 평가 점수가 아닙니다. 이후 주차의 페르소나·여정·IA·사용자 테스트는 이번 구현 범위에 포함하지 않았습니다.

## 실행

이 폴더에서 다음 명령을 실행한 뒤 `http://127.0.0.1:8767/uiux-lab.html`을 엽니다.

```sh
python3 -m http.server 8767 --bind 127.0.0.1
```

상단 **수업 모드**는 실험에 집중하도록 보조 영역을 접습니다. Escape로 종료합니다. 실험 링크를 복사하면 현재 원리로 바로 연결됩니다. 닐슨은 선택한 원칙 번호도 링크에 포함됩니다. 링크는 실험의 조절값이나 측정 기록을 저장하지 않습니다.

관찰 메모는 **기록 저장**을 눌러 현재 브라우저의 localStorage에 보관합니다. 다른 기기와 동기화되지 않습니다. **전체 기록 내려받기**는 현재까지 작성한 메모를 Markdown으로 저장합니다. 반응 시간·회상 점수 등 실험 측정치는 해당 실험을 열어둔 동안만 유지되므로, 남기고 싶은 결과는 관찰 메모에 적습니다.

## 파일 구성

- `uiux-lab.html / .css / .mjs`: 공통 화면, 실험 이동, 수업 모드, 메모
- `uiux-perception.mjs / .css`: 시지각 6개 실험
- `uiux-behavior.mjs / .css`: 사용성 법칙 4개 실험
- `uiux-review.mjs / .css`: 닐슨 10원칙 사례
- `penumbra-ui.css / .js`, `lab-navigation.css`: 기존 사이트의 공통 파일 사용

홈페이지 Work 카드와 기존 5개 Lab 메뉴에 새 페이지 링크를 추가했습니다. 작업은 원격 `codex/uiux-lab` 브랜치에 보관합니다. 공개 사이트에는 아직 배포하지 않았습니다.

## 확인한 항목

- 11개 모듈과 닐슨 10개 사례의 강의계획서 범위·설명 검수
- 실제 브라우저의 실험 전환, 공동운명 재생·정지, 피츠 클릭 기록, 밀러 가리기·회상 채점
- 닐슨의 상태 피드백, 오류 예방, 날짜별 복구 안내와 키보드 포커스 연결
- 모바일 390px에서 11개 모듈의 페이지 가로 넘침 없음
- 모바일 시간제 실험을 시작할 때 문제 화면으로 이동
- 메모 저장·새로고침 후 복원 및 실제 Markdown 다운로드
- 모듈 구문 검사, 로컬 파일 참조, 기존 페이지 링크, Git 공백 오류 검사

## 디자인

저채도 차콜 바탕에 라벤더로 제목과 선택 상태를 표시합니다. 실험 프레임·목차·버튼·값 배지의 모서리와 타이포그래피를 통일했고, 수업 모드에서는 실험과 관찰 질문이 함께 보이도록 높이를 조절했습니다. 도형의 조건, 피츠 목표의 크기·거리, 위계 조절 변수와 애니메이션 동작은 그대로 유지합니다.
